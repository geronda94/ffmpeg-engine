# 🎬 Системное руководство по FFmpeg Video Engine (v3.3)
> **ВНИМАНИЕ ДЛЯ LLM:** Данный документ является СТРОГОЙ инструкцией (System Prompt) по сборке JSON-конфигураций. Нарушение правил приведет к техническому браку видео.

---

## 🏗 Ментальная модель и архитектура
Движок работает как многослойная сборочная линия с жесткой изоляцией времени:
1. **Resources**: Объявление сырья.
2. **Pipeline**: Независимая обработка каждого слоя (внутреннее время сбрасывается в 0).
3. **Compose**: Финальная сборка слоев на глобальном таймлайне проекта.

---

## 🔴 СТРОГИЕ ПРАВИЛА (CRITICAL RULES)

### Правило 1: Управление таймлайном (Trim и src_trim)
- **`trim` (Позиция)**: Определяет, когда слой появится и исчезнет в ИТОГОВОМ видео.
- **`src_trim` (Источник)**: Определяет, какой именно кусок взять из ИСХОДНОГО файла. Если `src_trim` не указан, движок берет кусок из источника по тем же координатам времени, что и `trim`.

#### Пример "Нарезка из середины":
Вы хотите взять 5 секунд из 10-й минуты (600 сек) большого видео и поставить их в начало своего ролика:
```json
{
  "id": "my_clip",
  "input": "long_source",
  "trim": { "start": 0, "end": 5 },      // Будет в начале итогового видео
  "src_trim": { "start": 600, "end": 605 } // Вырезано из 10-й минуты исходника
}
```
- **Изоляция слоя**: После обрезки внутреннее время слоя сбрасывается в **ноль**. Все `actions` (fade, drawtext) отсчитываются от этого нуля.

### Правило 2: Сохранение Альфа-канала (Прозрачности)
- **Порядок экшенов**: Эффекты геометрии (scale, zoom) ВСЕГДА должны идти ДО эффектов прозрачности (dissolve, fade_in).
- **Подводный камень**: Фильтры `zoom` (особенно с `smooth: true`) принудительно переводят кадр в формат `yuv420p` для сглаживания. Если запустить зум после того, как слой стал полупрозрачным, прозрачность будет уничтожена (заменится черным фоном).
- **ПРАВИЛЬНО**: `scale` -> `zoom` -> `fade_in`.

### Правило 3: UI и Графические элементы
Движок поддерживает два типа графических ассетов. Выбор правильного типа ресурса критически важен для производительности и качества.

| Тип элемента | Значение `type` | Флаг в FFmpeg | Когда использовать |
| :--- | :--- | :--- | :--- |
| **Статичная плашка** | `image` | `-loop 1` | Подложки под текст, логотипы, иконки. |
| **Анимированный баннер** | `loop_video` | `-stream_loop -1` | GIF-реклама, WebM-градиенты, живые фоны. |
| **Фоновое видео** | `video` | (нет) | Основной контент, проигрывается один раз. |

#### Пример рекламной интеграции:
```json
{
  "resources": [
    { "id": "static_plate", "source": "local_assets/ui/plate_static.png", "type": "image" },
    { "id": "promo_banner", "source": "local_assets/ui/test_anim.webm", "type": "loop_video" }
  ],
  "pipeline": [
    {
      "id": "plate_layer",
      "input": "static_plate",
      "trim": { "start": 2, "end": 18 },
      "actions": [{ "type": "fade_in", "duration": 1.0, "alpha": true }]
    }
  ],
  "compose": {
    "base": "main_video",
    "layers": [
      { "source": "promo_banner", "pos": { "x": "50", "y": "50" } },
      { "source": "plate_layer", "pos": { "x": "(W-w)/2", "y": "800" } }
    ]
  }
}
```

- **ВАЖНО**: Для картинок и зацикленных видео используйте `trim` только для определения времени появления на экране. Движок сам позаботится о бесконечном зацикливании источника.

---

## 📄 Спецификация JSON блоков

### 1. `output`
- `fps`: Строго `30`.
- `codec`: `auto` или `libx264`.
- `duration`: Общая длительность проекта (должна совпадать с финалом последнего слоя).

### 2. `resources`
- `type: "image"`: Статичные фото и PNG-плашки. Флаг `-loop 1` заставляет FFmpeg удерживать этот кадр бесконечно, что позволяет показывать фото на экране любую длительность через `trim`.
- `type: "loop_video"`: Анимированные элементы (GIF, WebM). Флаг `-stream_loop -1` зацикливает анимацию, чтобы она не останавливалась, если слой длится дольше исходного файла.
- `type: "video"`: Обычные видео-исходники. Проигрываются один раз от начала до конца (или обрезаются через `src_trim`).
- `type: "lavfi"`: Генераторные источники (пустые холсты, цветовые заливки).

### 3. `pipeline`
Каждый слой — это независимый поток. Обязательно используйте `trim`, чтобы движок мог оптимизировать память.

### 4. `compose`
Массив `layers` определяет порядок: первый в списке — самый нижний.
- `pos`: `{"x": 0, "y": 0}` — левый верхний угол.
- Координаты поддерживают формулы: `(W-w)/2` для центровки.

### 5. `audio`
Звуки накладываются на глобальный таймлайн.
- `start`: Задержка от начала видео.
- `normalize: 0`: (Внутреннее правило) Всегда используется для сохранения динамического диапазона.

> **СОВЕТ ДЛЯ LLM**: Перед сборкой JSON вы можете сгенерировать озвучку из текста, используя утилиту `tools/tts_generator.py`. Полученный аудиофайл затем указывается в блоке `resources`.
